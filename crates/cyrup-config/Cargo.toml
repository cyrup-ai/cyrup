[package]
name = "cyrup-config"
description = "Layered settings, project trust, auth store, model resolution (arch-07)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
# `ConfiguredApiKeyAuth` implements cyrup-provider's `#[async_trait]` `ApiKeyAuth` (models.json →
# composed provider auth, provider_compose.rs).
async-trait = { workspace = true }
base64 = "0.22.1"
cyrup-core = { workspace = true }
cyrup-provider = { workspace = true }
directories = "6.0.0"
feruca = { workspace = true }
fs4 = "1.1.0"
oauth2 = "5.0.0"
serde.workspace = true
serde_json.workspace = true
sha2 = "0.11.0"
thiserror = { workspace = true }
tokio.workspace = true

[dev-dependencies]
# `tests/models_json_provider.rs` writes a real `models.json` into a tempdir and streams a request
# through the provider composed from it.
tempfile = { workspace = true }
