//! Tests for the settings tree, relocated verbatim from `settings.rs`'s single `mod tests`.
//! Split only so no file exceeds the module-size budget; every assertion is unchanged.

mod getters;
mod merge_and_scope;
mod write_refusal;
